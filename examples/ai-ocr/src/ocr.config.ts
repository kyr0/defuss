/** Shared OCR configuration — imported by both server (RPC) and client (UI). */

export const OCR_DEFAULT_PROMPT = "Text Recognition:";
export const OCR_DEFAULT_MODEL = "glm-ocr";
export const OCR_MAX_TOKENS = 4096;
