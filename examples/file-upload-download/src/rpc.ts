// Upload handlers are registered via side-effect imports (see src/api/file-upload.ts).
// This file exports an empty RPC namespace — the upload API is independent of the
// class/module RPC system.
export default {};
