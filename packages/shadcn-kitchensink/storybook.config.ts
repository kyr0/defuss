import { defineConfig } from "defuss-storybook";

export default defineConfig({
  themes: [
    { name: "Claude", className: "theme-claude" },
    { name: "Doom 64", className: "theme-doom-64" },
    { name: "Supabase", className: "theme-supabase" },
  ],
});
