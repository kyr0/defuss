# Retro Graphical User Interface (ReGUI) Architecture

# JSX components 

ReGUI uses defuss API's for rendering and state management.
The rendering backend is the DOM itself, while the styling and
theming is based on CSS.

