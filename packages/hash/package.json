{
  "name": "defuss-hash",
  "version": "0.1.0",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "description": "A library of fast and stable special-purpose hashing functions such as JSON content hashing in Rust + WebAssembly with skip paths, rendezvous hashing, etc.",
  "keywords": [
    "hash",
    "content-hash",
    "json",
    "wasm",
    "webassembly",
    "rust",
    "isomorphic",
    "deduplication",
    "defuss",
    "rendezvous-hash",
    "consistent-hash"
  ],
  "repository": {
    "url": "git+https://github.com/kyr0/defuss.git",
    "type": "git"
  },
  "scripts": {
    "setup": "bun run scripts/setup.ts",
    "clean": "rm -rf ./coverage ./dist ./wasm/pkg ./node_modules/.vite",
    "prebuild": "bun run clean",
    "build": "bun scripts/build.ts",
    "pretest": "bun run build",
    "test": "vitest run --coverage",
    "test:browser": "vitest run --config vitest.browser.config.ts",
    "bench": "vitest bench --run --config vitest.bench.config.ts",
    "pretest:browser": "bun run build",
    "prebench": "bun run build"
  },
  "author": "Aron Homberg",
  "sideEffects": false,
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      }
    },
    "./wasm": {
      "import": {
        "types": "./wasm/pkg/defuss_hash.d.ts",
        "default": "./wasm/pkg/defuss_hash.js"
      }
    }
  },
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "wasm/pkg",
    "rust/src/content.md",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": "^20.0.0 || >=22.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "@vitest/browser": "^4.0.18",
    "@vitest/browser-playwright": "^4.0.18",
    "@vitest/coverage-v8": "^4.0.18",
    "playwright": "^1.58.2",
    "typescript": "^5.9.3",
    "vite-plugin-wasm": "^3.5.0",
    "vitest": "^4.0.18"
  }
}
